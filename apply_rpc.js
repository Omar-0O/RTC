const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({path: '.env.local'}); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.rpc('check_room_conflict', { p_room: 'lab_1', p_schedule_days: ['monday'], p_schedule_time: '10:00', p_schedule_end_time: '12:00', p_start_date: '2026-05-01', p_end_date: '2026-05-30', p_exclude_course_id: null }); console.log('RPC exists?', !error || !error.message.includes('Could not find the function')); } test();
